# Web -- SPA (React)

> Arquetipo de referencia -- no pensado para ejecutarse como sistema real. Ver disclaimer completo en `../README.md`.

Ver PDF §5 y §7. La SPA nunca maneja el Access Token: toda la sesión se
resuelve vía cookie httpOnly gestionada por el BFF Web (`backend/bff-web`).

```bash
npm install
npm run dev
```
