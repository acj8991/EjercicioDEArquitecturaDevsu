# Mobile -- App React Native (Expo)

> Arquetipo de referencia -- no pensado para ejecutarse como sistema real. Ver disclaimer completo en `../README.md`.

Ver PDF §7 y §8: Authorization Code + PKCE nativo (`src/auth/pkce.ts`),
almacenamiento seguro de tokens (`src/auth/secureStorage.ts`) y onboarding
con verificación biométrica (`src/screens/OnboardingScreen.tsx`).

```bash
npm install
npm run start
```
